# DESIGN_STANDARDS — Стандарты дизайна

## Цвета
**Ожидает заполнения** — после получения предпочтений заказчика

## Шрифты
**Ожидает заполнения**

## Стиль
**Ожидает заполнения**

---

## Сетка и отступы (базовые стандарты)
- Максимальная ширина контента: 1200px
- Боковые отступы контейнера: 20px (мобайл), 40px (планшет), 80px (десктоп)
- Отступы между секциями: 80px (мобайл), 120px (десктоп)
- Отступы внутри секций между элементами: 24px–40px
- Базовая единица spacing: 8px (все отступы кратны 8)

## Типографика (базовые стандарты)
- H1: 36–48px, жирный
- H2: 28–36px, жирный
- H3: 20–24px, полужирный
- Body: 16–18px, нормальный
- Small: 14px
- Межстрочный интервал: 1.4–1.6

## Кнопки
- Высота: 48–56px
- Скругление: 8–12px
- Паддинг: 16px 32px
- CTA-кнопка: крупная, контрастный цвет, чёткий текст действия

## Адаптивность
- Мобайл: < 768px
- Планшет: 768–1024px
- Десктоп: > 1024px
- Mobile-first подход

## Анимации — сдержанные, плавные, живые

> Референсы: templifica.com, smoothui.dev

### Общий принцип
Анимации деликатные и профессиональные, но сайт должен ощущаться **живым**. Блоки реагируют на мышку, элементы слегка «дышат», при hover появляются мягкие подсветки и свечения.

### Появление блоков при скролле (reveal on scroll)
- Эффект: **fade-in + blur** (opacity 0 → 1, filter: blur(8px) → blur(0)) + лёгкий сдвиг снизу (translateY: 20–30px → 0)
- Длительность: 0.6–0.8s
- Easing: ease-out
- Триггер: Intersection Observer, порог 0.15
- Элементы появляются по одному с задержкой 0.1–0.15s между соседними

### Реакция на мышку (mouse-tracking)
- Карточки/блоки: лёгкий **tilt-эффект** — наклон в сторону курсора (rotateX/rotateY ±3–5deg), perspective: 800–1000px
- **Подсветка при hover** — мягкое свечение (radial-gradient следует за курсором внутри карточки)
- Реализация: JS mousemove + CSS transform, transition 0.15s ease-out
- При уходе курсора — плавный возврат в исходное положение

### Hover-эффекты
- Кнопки: плавная смена фона + мягкое свечение (box-shadow с цветом кнопки, spread 10–20px, opacity 0.3), transition 0.3s ease
- Карточки: подъём (translateY: -4px) + усиление тени + мягкая подсветка границ, transition 0.3s ease
- Ссылки: плавная смена цвета, transition 0.2s

### Микроанимации внутри блоков
- Декоративные элементы могут слегка «дышать» (scale 1 → 1.02 → 1, opacity мерцание 0.8 → 1 → 0.8)
- Длительность: 3–5s, ease-in-out, бесконечный цикл
- Применять **только** к декоративным элементам (фоновые градиенты, иконки-акценты), НЕ к тексту и контенту
- Доступность: `@media (prefers-reduced-motion: reduce)` — отключать все анимации

### CTA-кнопки
- Без агрессивной пульсации — только плавный hover с мягким свечением
- При наведении: усиление яркости + glow-тень цвета кнопки

### Переходы
- Все transition: 0.2–0.4s ease
- Никаких резких переключений состояний

### Запрещено
- Агрессивные эффекты (bounce, shake, flash)
- Анимации, замедляющие скролл
- Тяжёлые анимации, нагружающие GPU без необходимости

### Технология
- CSS transitions + CSS @keyframes для простых эффектов
- Intersection Observer API для reveal on scroll
- Чистый JS для mouse-tracking (mousemove + requestAnimationFrame)
- `will-change: transform` на анимируемых элементах
- `@media (prefers-reduced-motion: reduce)` — обязательно
- Без тяжёлых библиотек, чистый JS

## Промпты для генерации дизайна (Midjourney / AI)
**Ожидает заполнения** — будут добавлены после утверждения визуального стиля
