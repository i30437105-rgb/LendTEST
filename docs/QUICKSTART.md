# QUICKSTART — Быстрый старт для нового ассистента

> **Прочитай это первым.** Затем читай остальные документы из списка.

## Что это за проект

Лендинг для продукта "краш-тест маркетинга". Технология: чистый HTML/CSS/JS.

## Текущий статус (v47)

✅ **Готовы:** Экран 1 (Hero+сканер, v29), Экран 2 (блок спикера, v42), Экран 3 (бонусы, v47), Экран 4 (features, v45)
🔜 **Ожидают референсы:** Экран 5–N

## Обязательные документы для чтения

1. **PROJECT_GUIDE.md** — железные правила, дизайн-стандарты, бренд
2. **PROJECT_STYLE.md** — фиксация стиля: все CSS-переменные, цвета, типографика
3. **DESIGN_TRANSFER_PROTOCOL.md** — как работать со скриншотами референсов
4. **ERRORS.md** — таблица паттернов ошибок (НЕ повторять)
5. **WORKFLOW.md** — процесс работы, статус экранов
6. **CHANGELOG.md** — история изменений (v31–v47)
7. **COPY.md** — утверждённые тексты

## Главные правила (НАРУШАТЬ НЕЛЬЗЯ)

1. **Экран за экраном** — следующий экран ТОЛЬКО после утверждения предыдущего
2. **Референс = структура, НЕ стиль** — копируем layout, применяем наш визуальный стиль
3. **Протокол обязателен** — при работе со скриншотами следовать DESIGN_TRANSFER_PROTOCOL.md
4. **Не создавать графику** — иконки только из Lucide, сохранять в `/assets/icons/`
5. **Фиксировать всё** — изменения → CHANGELOG.md, замечания → ERRORS.md
6. **Десктоп ≠ мобайл** — мобильную версию корректировать отдельно, не масштабировать автоматически
7. **После утверждения** — обновить ВСЕ документы, закоммитить, запушить

## Ключевые параметры

**Типографика:** H1: Unbounded 55px/28px (вес 600), H2: Unbounded 22px/17px (вес 400), Body: Manrope 18px/14px (вес 400, цвет #7a7f8a)

**Цвета:** Фон #f5f5f7, текст #1a1a2e, акцент #c8f542, красный #ec5353

**Отступы:** Десктоп 150px между секциями, мобайл 80px. **Принцип:** верхняя секция даёт padding-bottom, нижняя — padding-top: 0

**Карточки:** border-radius 16px (карточка) / 12px (изображения) / 12px (padding) — ЖЕЛЕЗНОЕ ПРАВИЛО

## Локальный сервер

```bash
npx serve -l 3000 .
```

Открой http://localhost:3000

## Что делать дальше

Прочитай все документы из списка выше, затем скажи "Готов" и жди указаний заказчика.
